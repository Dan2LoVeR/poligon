# Анализируем содержимое документов для определения классов
# (здесь предполагается ручной анализ нескольких документов из каждого кластера)

# Пример автоматического определения (если темы документов можно определить по ключевым словам)
classes = []
# Создаем список для хранения классов документов
keywords = [
    ['технология', 'компьютер', 'программа'],  # Класс 1: технологии
    ['спорт', 'игра', 'матч'],                # Класс 2: спорт
    ['политика', 'правительство', 'закон']     # Класс 3: политика
]
# Список ключевых слов для каждого класса

for doc in documents:
    words = preprocess_document(doc)
    # Получаем очищенные и лемматизированные слова документа
    scores = [
        sum(1 for word in words if word in keywords[0]),
        sum(1 for word in words if word in keywords[1]),
        sum(1 for word in words if word in keywords[2])
    ]
    # Считаем количество ключевых слов каждого класса в документе
    classes.append(scores.index(max(scores)))
    # Определяем класс как индекс максимального значения в scores

print("Определенные классы документов:")
print(classes)
# Выводим полученные классы документов